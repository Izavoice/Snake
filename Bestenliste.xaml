<Window x:Class="WPF_Snake_Demo.Bestenliste"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Snake_Demo"
        mc:Ignorable="d"
        Title="Bestenliste" Height="400" Width="250" Topmost="True" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid x:Name="meinGrid">
        <!--Rows-->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!--Columns-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        
       
        <Label Grid.ColumnSpan="2" Content="Bestenliste" HorizontalAlignment="Center" FontSize="22 pt" FontWeight="Bold"/>
        
    </Grid>
</Window>
